{
  "name": "dyextract",
  "version": "1.0.0",
  "description": "DyExtract - 短视频无水印解析与预览工具（Cloudflare Pages 版本）",
  "scripts": {
    "pages:dev": "npx wrangler@latest pages dev public --port 8788 --kv VIDEO_CACHE",
    "pages:deploy": "npx wrangler@latest pages deploy public --project-name dyextract"
  },
  "private": true,
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
